# 02 · GPU Kernels

> 状态：尚未实现。本目录是 GPU 算子学习的唯一入口。

计划按 `PyTorch reference → naive CUDA/Triton → optimized CUDA/Triton → correctness → benchmark` 的顺序实现 Vector Add、ReLU、Softmax、LayerNorm、MatMul 和 Attention。

进入本模块前，请先完成 [01 · PyTorch Training](../01_pytorch_training/README.md)。代码、测试、配置、benchmark 和实验报告都应保留在本目录内，不建立另一套顶层里程碑目录。
